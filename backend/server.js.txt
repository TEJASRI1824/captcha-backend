const express = require("express");
const cors = require("cors");

const app = express();
app.use(express.json());
app.use(cors());

let users = [];

app.post("/update", (req, res) => {
    res.json({ earnings: 1, tasks: 1 });
});

app.listen(5000, () => console.log("Server running on port 5000"));